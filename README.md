This project uses NHL Player data to predict how well each NHL hockey player would do on a fantasy team.

The first draft is complete. I'm using the next few days to try to optimize and clean up the functionality of the code, then to improve the markup.